
RESEND_API_KEY=re_xxxxxxxxxx

# Canonical production URL — used for metadataBase, sitemap.xml, robots.txt,
# and Open Graph/Twitter social preview URLs. Update this once a custom domain
# is connected; until then it should match the live Vercel deployment URL.
NEXT_PUBLIC_SITE_URL=https://kemma-technologies.vercel.app
